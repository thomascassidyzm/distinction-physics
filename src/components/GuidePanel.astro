---
// GuidePanel - Alexander epistemic guide for Distinction Physics
---

<aside id="guide-panel" class="guide-panel">
  <header class="guide-header">
    <div class="guide-identity">
      <span class="guide-dot"></span>
      <span class="guide-name">Alexander</span>
      <span class="guide-role">Epistemic Guide</span>
    </div>
    <div class="context-display">
      <span class="context-label">Currently Reading:</span>
      <span id="guide-section" class="context-section">Foundations</span>
      <span id="guide-status" class="status-badge established">established</span>
    </div>
  </header>

  <div id="guide-messages" class="messages-container">
    <div class="guide-welcome">
      <p>I'm Alexander, here to help you build understanding of Distinction Physics.</p>
      <p>I teach through distinction-building: what's <strong>similar</strong> to what you know, what's <strong>different</strong>, and how the pieces connect.</p>
    </div>
  </div>

  <div class="guide-suggestions" id="guide-suggestions">
    <button class="suggestion" data-query="What are the two axioms?">Two axioms</button>
    <button class="suggestion" data-query="What is an OLU?">What's an OLU?</button>
    <button class="suggestion" data-query="Explain this section simply">Explain simply</button>
  </div>

  <form id="guide-form" class="guide-form">
    <input
      type="text"
      id="guide-input"
      class="guide-input"
      placeholder="Ask about this section..."
      autocomplete="off"
    />
    <button type="submit" class="guide-submit" aria-label="Send">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M22 2L11 13M22 2l-7 20-4-9-9-4 20-7z"/>
      </svg>
    </button>
  </form>
</aside>

<button id="guide-mobile-trigger" class="guide-mobile-trigger" aria-label="Open Guide">
  <span class="guide-dot"></span>
</button>

<style is:global>
  .guide-panel {
    position: fixed;
    top: 60px;
    right: 0;
    bottom: 0;
    width: 420px;
    background: var(--depth-1, #0d0d12);
    border-left: 1px solid var(--border, rgba(255,255,255,0.06));
    display: flex;
    flex-direction: column;
    z-index: 50;
  }

  .guide-header {
    padding: 1.25rem 1.5rem;
    border-bottom: 1px solid var(--border, rgba(255,255,255,0.06));
    background: rgba(0,0,0,0.2);
  }

  .guide-identity {
    display: flex;
    align-items: center;
    gap: 0.625rem;
    margin-bottom: 1rem;
  }

  .guide-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--accent, #4a9eff);
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 0.6; transform: scale(1); }
    50% { opacity: 1; transform: scale(1.1); }
  }

  .guide-name {
    font-family: var(--font-display, 'Cormorant Garamond', serif);
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--text-primary, #e8e8f0);
  }

  .guide-role {
    font-size: 0.6875rem;
    color: var(--text-muted, #606080);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-left: 0.25rem;
  }

  .context-display {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .context-label {
    font-size: 0.6875rem;
    color: var(--text-muted, #606080);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .context-section {
    font-size: 0.875rem;
    color: var(--text-secondary, #a0a0b8);
  }

  .status-badge {
    font-size: 0.625rem;
    padding: 0.125rem 0.5rem;
    border-radius: 3px;
    text-transform: lowercase;
    font-weight: 500;
  }

  .status-badge.established { background: rgba(74, 158, 255, 0.15); color: #4a9eff; }
  .status-badge.derived { background: rgba(80, 200, 120, 0.15); color: #50c878; }
  .status-badge.contested { background: rgba(255, 165, 0, 0.15); color: #ffa500; }
  .status-badge.open { background: rgba(168, 85, 247, 0.15); color: #a855f7; }

  .messages-container {
    flex: 1;
    overflow-y: auto;
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .guide-welcome {
    font-size: 0.9375rem;
    color: var(--text-secondary, #a0a0b8);
    line-height: 1.7;
  }

  .guide-welcome p {
    margin-bottom: 0.75rem;
  }

  .guide-welcome p:last-child {
    margin-bottom: 0;
  }

  .message {
    padding: 0.875rem 1rem;
    border-radius: 10px;
    font-size: 0.9375rem;
    line-height: 1.7;
    max-width: 95%;
  }

  .message.user {
    background: var(--accent, #4a9eff);
    color: white;
    align-self: flex-end;
    border-bottom-right-radius: 4px;
  }

  .message.assistant {
    background: rgba(255,255,255,0.04);
    color: var(--text-primary, #e8e8f0);
    align-self: flex-start;
    border-bottom-left-radius: 4px;
    border: 1px solid rgba(255,255,255,0.06);
  }

  .message.assistant p { margin: 0 0 0.75em 0; }
  .message.assistant p:last-child { margin-bottom: 0; }
  .message.assistant strong { color: var(--text-primary, #e8e8f0); font-weight: 600; }
  .message.assistant em { font-style: italic; opacity: 0.9; }
  .message.assistant ul, .message.assistant ol { margin: 0.5em 0; padding-left: 1.5em; }
  .message.assistant li { margin-bottom: 0.25em; }
  .message.assistant code {
    background: rgba(255,255,255,0.1);
    padding: 0.15em 0.4em;
    border-radius: 3px;
    font-family: var(--font-mono, 'JetBrains Mono', monospace);
    font-size: 0.85em;
  }

  /* Follow-up buttons - subtle pill style */
  .message.assistant .follow-ups {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 1rem;
    padding-top: 0.875rem;
    border-top: 1px solid rgba(255,255,255,0.06);
  }

  .message.assistant .follow-up-btn {
    font-size: 0.75rem;
    padding: 0.375rem 0.75rem;
    border-radius: 20px;
    border: 1px solid rgba(255,255,255,0.1);
    background: transparent;
    color: var(--text-secondary, #a0a0b8);
    cursor: pointer;
    transition: all 0.2s ease;
    font-family: inherit;
  }

  .message.assistant .follow-up-btn:hover {
    border-color: var(--accent, #4a9eff);
    color: var(--accent, #4a9eff);
  }

  /* Typing indicator */
  .typing-indicator {
    display: flex;
    align-items: center;
    gap: 4px;
    padding: 4px 0;
  }

  .typing-indicator span {
    width: 6px;
    height: 6px;
    background: var(--accent, #4a9eff);
    border-radius: 50%;
    animation: typing-pulse 1.2s infinite ease-in-out;
  }

  .typing-indicator span:nth-child(1) { animation-delay: 0s; }
  .typing-indicator span:nth-child(2) { animation-delay: 0.15s; }
  .typing-indicator span:nth-child(3) { animation-delay: 0.3s; }

  @keyframes typing-pulse {
    0%, 60%, 100% { opacity: 0.3; transform: scale(1); }
    30% { opacity: 1; transform: scale(1.15); }
  }

  .guide-suggestions {
    padding: 1rem 1.5rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    border-top: 1px solid rgba(255,255,255,0.04);
    background: rgba(0,0,0,0.1);
  }

  .guide-suggestions.hidden {
    display: none;
  }

  .suggestion {
    font-size: 0.8125rem;
    padding: 0.5rem 0.875rem;
    border-radius: 20px;
    border: 1px solid rgba(74, 158, 255, 0.2);
    background: transparent;
    color: rgba(74, 158, 255, 0.6);
    cursor: pointer;
    transition: all 0.2s ease;
    font-family: inherit;
  }

  .suggestion:hover {
    border-color: rgba(74, 158, 255, 0.5);
    background: rgba(74, 158, 255, 0.08);
    color: #4a9eff;
  }

  .guide-form {
    padding: 1.25rem 1.5rem;
    border-top: 1px solid rgba(255,255,255,0.06);
    display: flex;
    gap: 0.625rem;
    background: rgba(0,0,0,0.2);
  }

  .guide-input {
    flex: 1;
    padding: 0.875rem 1rem;
    border-radius: 10px;
    border: 1px solid rgba(255,255,255,0.1);
    background: rgba(255,255,255,0.03);
    color: var(--text-primary, #e8e8f0);
    font-size: 0.9375rem;
    font-family: inherit;
    outline: none;
    transition: border-color 0.2s ease;
  }

  .guide-input::placeholder {
    color: var(--text-muted, #606080);
  }

  .guide-input:focus {
    border-color: var(--accent, #4a9eff);
  }

  .guide-submit {
    padding: 0.75rem 0.875rem;
    border-radius: 8px;
    border: none;
    background: var(--accent, #4a9eff);
    color: white;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: opacity 0.2s ease;
  }

  .guide-submit:hover {
    opacity: 0.9;
  }

  .guide-submit:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  .guide-mobile-trigger {
    display: none;
    position: fixed;
    bottom: 1.5rem;
    right: 1.5rem;
    width: 48px;
    height: 48px;
    border-radius: 50%;
    border: 1px solid rgba(255,255,255,0.1);
    background: var(--depth-1, #12121a);
    cursor: pointer;
    align-items: center;
    justify-content: center;
    z-index: 1000;
    box-shadow: 0 4px 20px rgba(0,0,0,0.3);
  }

  @media (max-width: 1100px) {
    .guide-panel {
      position: fixed;
      top: auto;
      bottom: 80px;
      right: 1rem;
      width: calc(100vw - 2rem);
      max-width: 400px;
      max-height: 70vh;
      border-radius: 12px;
      border: 1px solid rgba(255,255,255,0.1);
      display: none;
      box-shadow: 0 10px 40px rgba(0,0,0,0.4);
    }

    .guide-panel.mobile-open {
      display: flex;
    }

    .guide-mobile-trigger {
      display: flex;
    }
  }
</style>

<script>
  // Simple markdown parser
  function parseMarkdown(text: string): string {
    let html = text;
    html = html.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
    html = html.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
    html = html.replace(/\*([^*]+)\*/g, '<em>$1</em>');
    html = html.replace(/`([^`]+)`/g, '<code>$1</code>');
    html = html.replace(/^[\-\*]\s+(.+)$/gm, '<li>$1</li>');
    html = html.replace(/(<li>.*<\/li>\n?)+/g, '<ul>$&</ul>');
    html = html.split(/\n\n+/).map(p => {
      p = p.trim();
      if (p && !p.startsWith('<ul>')) {
        return `<p>${p}</p>`;
      }
      return p;
    }).join('');
    html = html.replace(/([^>])\n([^<])/g, '$1<br>$2');
    return html;
  }

  document.addEventListener('DOMContentLoaded', () => {
    const panel = document.getElementById('guide-panel');
    const mobileTrigger = document.getElementById('guide-mobile-trigger');
    const form = document.getElementById('guide-form');
    const input = document.getElementById('guide-input') as HTMLInputElement;
    const messagesContainer = document.getElementById('guide-messages');
    const suggestionsContainer = document.getElementById('guide-suggestions');
    const suggestions = document.querySelectorAll('.suggestion');
    const sectionEl = document.getElementById('guide-section');
    const statusEl = document.getElementById('guide-status');

    const STORAGE_KEY = 'distinction-physics-conversation';
    let conversationHistory: Array<{role: 'user' | 'assistant', content: string}> = [];
    let isLoading = false;

    // Restore previous conversation
    try {
      const stored = sessionStorage.getItem(STORAGE_KEY);
      if (stored) {
        conversationHistory = JSON.parse(stored);
        const welcome = messagesContainer?.querySelector('.guide-welcome');
        if (welcome && conversationHistory.length > 0) {
          welcome.remove();
          suggestionsContainer?.classList.add('hidden');
          conversationHistory.forEach(msg => {
            const el = document.createElement('div');
            el.className = `message ${msg.role}`;
            if (msg.role === 'assistant') {
              el.innerHTML = parseMarkdown(msg.content);
            } else {
              el.textContent = msg.content;
            }
            messagesContainer?.appendChild(el);
          });
          scrollToBottom();
        }
      }
    } catch (e) {}

    function saveConversation() {
      try {
        sessionStorage.setItem(STORAGE_KEY, JSON.stringify(conversationHistory));
      } catch (e) {}
    }

    // Mobile toggle
    mobileTrigger?.addEventListener('click', () => {
      panel?.classList.toggle('mobile-open');
    });

    function getCurrentContext() {
      return {
        sectionId: (window as any).currentSectionId || null,
        section: (window as any).currentSection || sectionEl?.textContent || 'Foundations',
        status: (window as any).currentStatus || 'established'
      };
    }

    function updateDisplayedContext() {
      const section = (window as any).currentSection;
      const status = (window as any).currentStatus;
      if (sectionEl && section) sectionEl.textContent = section;
      if (statusEl && status) {
        statusEl.textContent = status;
        statusEl.className = `status-badge ${status}`;
      }
    }

    setInterval(updateDisplayedContext, 300);

    function scrollToBottom() {
      if (messagesContainer) {
        messagesContainer.scrollTop = messagesContainer.scrollHeight;
      }
    }

    // Follow-up buttons
    function createFollowUpButtons(): HTMLElement {
      const container = document.createElement('div');
      container.className = 'follow-ups';
      const options = [
        { label: 'Say more', query: 'Can you expand on that?' },
        { label: 'Related', query: 'What concepts relate to this?' },
        { label: 'What follows?', query: 'What are the logical consequences?' },
      ];
      options.forEach(opt => {
        const btn = document.createElement('button');
        btn.className = 'follow-up-btn';
        btn.textContent = opt.label;
        btn.addEventListener('click', () => {
          document.querySelectorAll('.follow-ups').forEach(el => el.remove());
          sendMessage(opt.query);
        });
        container.appendChild(btn);
      });
      return container;
    }

    // Suggestions
    suggestions.forEach(btn => {
      btn.addEventListener('click', () => {
        const query = btn.getAttribute('data-query');
        if (query) sendMessage(query);
      });
    });

    form?.addEventListener('submit', (e) => {
      e.preventDefault();
      const message = input?.value.trim();
      if (message && !isLoading) {
        sendMessage(message);
        if (input) input.value = '';
      }
    });

    async function sendMessage(message: string) {
      if (isLoading) return;
      isLoading = true;

      const welcome = messagesContainer?.querySelector('.guide-welcome');
      if (welcome) welcome.remove();
      suggestionsContainer?.classList.add('hidden');

      // User message
      const userEl = document.createElement('div');
      userEl.className = 'message user';
      userEl.textContent = message;
      messagesContainer?.appendChild(userEl);
      scrollToBottom();

      // Typing indicator
      const typingEl = document.createElement('div');
      typingEl.className = 'message assistant';
      typingEl.innerHTML = '<div class="typing-indicator"><span></span><span></span><span></span></div>';
      messagesContainer?.appendChild(typingEl);
      scrollToBottom();

      const context = getCurrentContext();

      try {
        const response = await fetch('/api/guide', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            message,
            history: conversationHistory,
            context: {
              currentSection: context.sectionId || context.section,
              currentSectionTitle: context.section,
              epistemicStatus: context.status
            }
          })
        });

        if (!response.ok) throw new Error('Guide unavailable');

        const data = await response.json();
        typingEl.innerHTML = parseMarkdown(data.message);
        typingEl.appendChild(createFollowUpButtons());
        scrollToBottom();

        conversationHistory.push(
          { role: 'user', content: message },
          { role: 'assistant', content: data.message }
        );

        if (conversationHistory.length > 10) {
          conversationHistory = conversationHistory.slice(-10);
        }

        saveConversation();

      } catch (error) {
        typingEl.remove();
        const errorEl = document.createElement('div');
        errorEl.className = 'message assistant';
        errorEl.innerHTML = '<p>I apologize, but I\'m having trouble connecting. Please try again.</p>';
        messagesContainer?.appendChild(errorEl);
        scrollToBottom();
      }

      isLoading = false;
    }
  });
</script>
