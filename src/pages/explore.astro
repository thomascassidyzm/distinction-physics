---
import SiteHeader from '../components/SiteHeader.astro';
import DistinctionNetwork from '../components/DistinctionNetwork.astro';
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Explore the Distinction Network · Distinction as Primitive</title>
  <meta name="description" content="Explore the concepts of Distinction as Primitive as an interactive network. Mark concepts as understood and watch your constellation of knowledge illuminate.">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,400&family=Sora:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
</head>
<body>
  <SiteHeader currentPage="explore" />

  <main>
    <DistinctionNetwork />
  </main>
</body>
</html>

<style>
  :root {
    --void: #0a0a0c;
    --depth-1: #12121a;
    --depth-2: #1a1a24;
    --border: #2a2a3a;
    --text-primary: #e8e8f0;
    --text-secondary: #a0a0b8;
    --text-muted: #606080;
    --accent: #4a9eff;
    --font-display: 'Cormorant Garamond', serif;
    --font-body: 'Sora', sans-serif;
    --font-mono: 'JetBrains Mono', monospace;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html, body {
    height: 100%;
    overflow: hidden;
  }

  body {
    font-family: var(--font-body);
    background: var(--void);
    color: var(--text-primary);
    display: flex;
    flex-direction: column;
  }

  main {
    flex: 1;
    padding-top: 56px;
  }
</style>
