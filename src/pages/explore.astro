---
import { propositions } from '../content/propositions';
import { VERSION, BUILD } from '../lib/version';
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Explore Concepts · Distinction Physics</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,400&family=Sora:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
</head>
<body>
  <header class="site-header">
    <nav class="header-nav">
      <a href="/" class="site-title">Distinction Physics</a>
      <span class="separator">/</span>
      <span class="page-title">Explore Concepts</span>
    </nav>
    <div class="header-meta">
      <span class="count">{propositions.length} propositions</span>
      <a href="/essay" class="back-link">← Back to Essay</a>
    </div>
  </header>

  <main class="main-content">
    <div class="intro">
      <h1>Concept Network</h1>
      <p>Each proposition in the framework is a node in a network of ideas. Click any card to expand its logical structure and see its connections.</p>
    </div>

    <div class="propositions-grid">
      {propositions.map((prop) => (
        <article class="proposition-card" data-id={prop.id}>
          <header class="card-header">
            <h2>{prop.title}</h2>
            <span class="version">{prop.version}</span>
          </header>
          <p class="surface">{prop.surface}</p>

          <div class="logic-panel hidden">
            <div class="logic-section">
              <span class="logic-label">Claim</span>
              <p class="logic-claim">{prop.logic.claim}</p>
            </div>

            <div class="logic-section">
              <span class="logic-label">Premises</span>
              <ul class="premises">
                {prop.logic.premises.map(premise => (
                  <li>{premise}</li>
                ))}
              </ul>
            </div>

            <div class="logic-section">
              <span class="logic-label">Conclusion</span>
              <p class="logic-conclusion">{prop.logic.conclusion}</p>
            </div>

            {prop.logic.predictive && (
              <div class="logic-section">
                <span class="logic-label">Prediction</span>
                <p class="logic-prediction">{prop.logic.predictive}</p>
              </div>
            )}

            {prop.linksTo.length > 0 && (
              <div class="links-section">
                <span class="logic-label">Connected To</span>
                <div class="links">
                  {prop.linksTo.map(linkId => {
                    const linked = propositions.find(p => p.id === linkId);
                    return linked ? (
                      <button class="link-chip" data-target={linkId}>
                        {linked.title}
                      </button>
                    ) : null;
                  })}
                </div>
              </div>
            )}
          </div>

          <button class="expand-btn">
            <span class="expand-text">Show Logic</span>
            <svg class="expand-icon" width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M4 6L8 10L12 6" stroke="currentColor" stroke-width="2"/>
            </svg>
          </button>
        </article>
      ))}
    </div>
  </main>

  <footer class="site-footer">
    <span class="version-info">{VERSION} · build {BUILD}</span>
  </footer>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const cards = document.querySelectorAll('.proposition-card');

      cards.forEach(card => {
        const btn = card.querySelector('.expand-btn');
        const panel = card.querySelector('.logic-panel');
        const text = btn?.querySelector('.expand-text');
        const icon = btn?.querySelector('.expand-icon');

        btn?.addEventListener('click', () => {
          const isExpanded = !panel?.classList.contains('hidden');
          panel?.classList.toggle('hidden');

          if (text) {
            text.textContent = isExpanded ? 'Show Logic' : 'Hide Logic';
          }
          if (icon) {
            icon.style.transform = isExpanded ? 'rotate(0)' : 'rotate(180deg)';
          }
        });

        // Link chip navigation
        const linkChips = card.querySelectorAll('.link-chip');
        linkChips.forEach(chip => {
          chip.addEventListener('click', (e) => {
            e.stopPropagation();
            const targetId = chip.getAttribute('data-target');
            const targetCard = document.querySelector(`[data-id="${targetId}"]`);

            if (targetCard) {
              targetCard.scrollIntoView({ behavior: 'smooth', block: 'center' });

              // Expand the target card
              const targetPanel = targetCard.querySelector('.logic-panel');
              const targetText = targetCard.querySelector('.expand-text');
              const targetIcon = targetCard.querySelector('.expand-icon');

              if (targetPanel?.classList.contains('hidden')) {
                targetPanel.classList.remove('hidden');
                if (targetText) targetText.textContent = 'Hide Logic';
                if (targetIcon) (targetIcon as HTMLElement).style.transform = 'rotate(180deg)';
              }

              // Highlight briefly
              targetCard.classList.add('highlighted');
              setTimeout(() => targetCard.classList.remove('highlighted'), 2000);
            }
          });
        });
      });
    });
  </script>
</body>
</html>

<style>
  :root {
    --void: #0a0a0c;
    --depth-1: #12121a;
    --depth-2: #1a1a24;
    --border: #2a2a3a;
    --text-primary: #e8e8f0;
    --text-secondary: #a0a0b8;
    --text-muted: #606080;
    --accent: #4a9eff;
    --accent-dim: rgba(74, 158, 255, 0.15);
    --font-display: 'Cormorant Garamond', serif;
    --font-body: 'Sora', sans-serif;
    --font-mono: 'JetBrains Mono', monospace;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    font-family: var(--font-body);
    background: var(--void);
    color: var(--text-primary);
    line-height: 1.6;
    min-height: 100vh;
  }

  .site-header {
    position: sticky;
    top: 0;
    background: var(--depth-1);
    border-bottom: 1px solid var(--border);
    padding: 1rem 2rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    z-index: 50;
  }

  .header-nav {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .site-title {
    font-family: var(--font-display);
    font-size: 1.125rem;
    color: var(--text-primary);
    text-decoration: none;
  }

  .separator {
    color: var(--text-muted);
  }

  .page-title {
    color: var(--text-secondary);
    font-size: 0.9375rem;
  }

  .header-meta {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    font-size: 0.875rem;
  }

  .count {
    color: var(--text-muted);
    font-family: var(--font-mono);
    font-size: 0.75rem;
  }

  .back-link {
    color: var(--accent);
    text-decoration: none;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  .main-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 3rem 2rem;
  }

  .intro {
    text-align: center;
    margin-bottom: 3rem;
  }

  .intro h1 {
    font-family: var(--font-display);
    font-size: 2.5rem;
    font-weight: 500;
    margin-bottom: 0.75rem;
  }

  .intro p {
    color: var(--text-secondary);
    max-width: 600px;
    margin: 0 auto;
  }

  .propositions-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(360px, 1fr));
    gap: 1.5rem;
  }

  .proposition-card {
    background: var(--depth-1);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 1.5rem;
    transition: all 0.2s;
  }

  .proposition-card:hover {
    border-color: var(--accent);
  }

  .proposition-card.highlighted {
    border-color: var(--accent);
    box-shadow: 0 0 0 2px var(--accent-dim);
  }

  .card-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 0.75rem;
  }

  .card-header h2 {
    font-family: var(--font-display);
    font-size: 1.25rem;
    font-weight: 500;
    line-height: 1.3;
  }

  .version {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    color: var(--text-muted);
    background: var(--depth-2);
    padding: 0.125rem 0.375rem;
    border-radius: 3px;
    flex-shrink: 0;
  }

  .surface {
    font-size: 0.9375rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  .logic-panel {
    border-top: 1px solid var(--border);
    padding-top: 1rem;
    margin-top: 1rem;
  }

  .logic-panel.hidden {
    display: none;
  }

  .logic-section {
    margin-bottom: 1rem;
  }

  .logic-label {
    display: block;
    font-size: 0.625rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-muted);
    margin-bottom: 0.375rem;
  }

  .logic-claim {
    font-size: 0.9375rem;
    color: var(--text-primary);
    font-weight: 500;
  }

  .premises {
    list-style: none;
    font-size: 0.875rem;
    color: var(--text-secondary);
  }

  .premises li {
    padding-left: 1rem;
    position: relative;
    margin-bottom: 0.25rem;
  }

  .premises li::before {
    content: '∴';
    position: absolute;
    left: 0;
    color: var(--accent);
  }

  .logic-conclusion {
    font-size: 0.9375rem;
    color: var(--text-primary);
  }

  .logic-prediction {
    font-size: 0.875rem;
    color: #50c878;
    font-style: italic;
  }

  .links-section {
    margin-top: 1rem;
    padding-top: 1rem;
    border-top: 1px solid var(--border);
  }

  .links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .link-chip {
    padding: 0.25rem 0.75rem;
    background: var(--accent-dim);
    border: 1px solid transparent;
    border-radius: 20px;
    color: var(--accent);
    font-size: 0.75rem;
    cursor: pointer;
    transition: all 0.2s;
  }

  .link-chip:hover {
    border-color: var(--accent);
    background: rgba(74, 158, 255, 0.25);
  }

  .expand-btn {
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.5rem;
    background: transparent;
    border: 1px solid var(--border);
    border-radius: 6px;
    color: var(--text-muted);
    font-size: 0.75rem;
    cursor: pointer;
    transition: all 0.2s;
  }

  .expand-btn:hover {
    border-color: var(--text-secondary);
    color: var(--text-secondary);
  }

  .expand-icon {
    transition: transform 0.2s;
  }

  .site-footer {
    padding: 2rem;
    text-align: center;
    border-top: 1px solid var(--border);
  }

  .version-info {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-muted);
  }

  @media (max-width: 640px) {
    .propositions-grid {
      grid-template-columns: 1fr;
    }

    .site-header {
      flex-direction: column;
      gap: 0.75rem;
      align-items: flex-start;
    }
  }
</style>
